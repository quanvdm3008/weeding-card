# Empty means same-origin; dev uses Vite proxy.
VITE_API_BASE_URL=
VITE_API_PROXY_TARGET=http://localhost:8080
VITE_SITE_URL=http://localhost:5173

# Set true only when you want the frontend to run without the backend.
VITE_USE_MOCK_API=false
